# Frontend

This folder now contains the React + Tailwind source for the premium frontend.

Key files:

- `src/App.jsx`
- `src/main.jsx`
- `src/index.css`
- `vite.config.js`

Build output is written to `../static/dist` so Flask can serve the compiled assets.
